# Keyvia sandbox

A small expense tracker that exists so Keyvia can hand stories to a coding agent and check its pull requests against their acceptance criteria. Nothing here is a real product.

```bash
npm test
```
