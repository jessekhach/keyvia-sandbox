# Working in this repository

- Plain JavaScript modules, Node 22 or later, no dependencies. Keep it that way unless the issue asks otherwise.
- Code lives in `src/`, tests in `test/`. Run `npm test` before you push.
- Keep each change to the issue you were given. Add or update tests for it.
- Amounts are integer cents. Dates are `YYYY-MM-DD` strings.
